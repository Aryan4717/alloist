# Alloist pilot deployment
# Copy to .env before running: cp .env.example .env

# API keys for token and policy services (change in production)
TOKEN_SERVICE_API_KEY=dev-api-key
POLICY_SERVICE_API_KEY=dev-api-key

# JWT signing secret for sessions (change in production)
JWT_SECRET=change-me-in-production
